# Development listener. Production mounts Caddyfile.tailscale instead (ADR-0007).
{
	admin off
	# The admin API is off: it can reconfigure the proxy, and nothing should be able
	# to reach it (threat model T2).
}

import /etc/caddy/healthcurve-app.caddy

:80 {
	import healthcurve_app
}
